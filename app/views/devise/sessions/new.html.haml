.AccountPage
  = link_to image_tag('logo.png',  class: 'pic1'), root_path
  .AccountPage__snsBox
    %h2.login-text 連携済アカウントでログイン
    .AccountPage__google
      = link_to image_tag 'apple.png', alt: '失敗', height: '50px', width: '100%', class: 'login-logo'
    .AccountPage__twitter
      = link_to image_tag 'google.png', alt: '失敗', height: '50px', width: '100%', class: 'login-logo'
    .AccountPage__facebook
      = link_to image_tag 'facebook.png', alt: '失敗', height: '50px', width: '100%', class: 'login-logo'
  .AccountPage__form
    %h5.or-logo もしくは
    %br
    %h2.login-text メールアドレスでログイン
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .FormField
        .FormField__label--normal
          = f.label :email
        .FormField__input--normal
          = f.email_field :email, autofocus: true
        .FormField
          .FormField__label--normal
            = f.label :password
            %i (英数字7文字以上)
          .FormField__input--normal
            = f.password_field :password, autocomplete: "off"
      .actions
        = f.submit "ログイン", class: 'Button UserButton'
    .AccountPage__title
    %h5.NoAcount アカウントをお持ちでない方は
  = render "devise/shared/links"